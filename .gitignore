node_modules/
.env
.DS_Store
Thumbs.db
desktop.ini
*.log
.github/workflows/

# Note: js/config.js is committed because GitHub Pages is static (no build step).
# If you switch to Cloudflare Pages, add js/config.js to gitignore and use env vars.

# Supabase local
supabase/.temp/
